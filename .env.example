# Beget account credentials used by acme.sh's dns_beget plugin for the
# Let's Encrypt DNS-01 challenge (required for wildcard certs - HTTP-01
# can't issue them). Enable API access first: Beget control panel ->
# "Настройки аккаунта" -> "Ограничение доступа" -> "Beget API" -> включить
# "Разрешить авторизацию по API".
Beget_Username=
Beget_Password=
